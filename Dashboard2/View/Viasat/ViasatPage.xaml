<Page x:Class="Dashboard2.View.Viasat.ViasatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
       xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:Dashboard2.View.Viasat"
      mc:Ignorable="d" 
      Title="ViasatPage"
      d:DesignHeight="800" d:DesignWidth="1230">


    <Grid Margin="0" Background="#EDF2FF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>

        <!-- PAGE TITLE -->
        <TextBlock Grid.Row="0" Style="{DynamicResource PageTitleTextBlock}" Text="Viasat"/>

        <!-- SEPARATOR -->
        <Separator Grid.Row="1" Height="1" Background="Transparent" Margin="0,-1,0,10"/>


        <Canvas>
            <TextBox x:Name="TextblockOwn" Width="150" Height="40" Canvas.Left="71" Canvas.Top="94" >

            </TextBox>


            <Button x:Name="ButtonOwn" Content="Button" Canvas.Left="248" Canvas.Top="104" HorizontalAlignment="Left"
                        VerticalAlignment="Center"   />

            <ListView x:Name="ListCheckpointOwn"  FontSize="12" Height="450" Width="828" 
                          Canvas.Left="344" Canvas.Top="151" HorizontalAlignment="Left" VerticalAlignment="Center"
                          ItemsSource="{Binding ListOfSummaryResultForSelectedCar, ElementName=TestElementName}">

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Czas Odczytu" DisplayMemberBinding="{Binding Path=Item[0]}" Width="Auto"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Path=Item[1]}" Width="Auto" />
                        <GridViewColumn Header="Przebieg" DisplayMemberBinding="{Binding Path=Item[2]}" Width="Auto"/>
                        <GridViewColumn Header="Przybliżony adres" DisplayMemberBinding="{Binding Path=Item[3]}" Width="Auto"/>

                    </GridView>
                </ListView.View>
            </ListView>


            <ListView  x:Name="ListOwn"  DisplayMemberPath="Name" FontSize="14" Background="AliceBlue"   
                          Width="304" Height="364" HorizontalAlignment="Left" Canvas.Left="10" Canvas.Top="151" 
                          VerticalAlignment="Center"  >
                <ListView.View  >
                    <GridView x:Name="grdTest"  >
                        <GridViewColumn  Header="RegNum" DisplayMemberBinding="{Binding Name }"  Width="150"/>
                        <GridViewColumn  Header="Id Device" DisplayMemberBinding="{Binding Id}" Width="140"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </Canvas>

    </Grid>

   
           

</Page>

